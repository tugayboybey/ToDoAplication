@model IEnumerable<ToDoAplication.Models.ToDo>

@{
    ViewData["Title"] = "TamamlananGorev";
}

<h1>Tamamlanan Gorevler </h1>


@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Todo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Todo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                <a asp-action="TamamlananGorevEdit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@
<table class="table table-bordered dataTable-top">
   
    <thead class=" border-0 ">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Todo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Todo)
            </td>
              <td>
                @Html.DisplayFor(modelItem => item.Comment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
           
            <td>
                 <a class="btn btn-warning" asp-action="TamamlananGorevEdit" asp-route-id="@item.ID">Görevi Düzenle</a> |
                
                <a class=" btn btn-danger" asp-action="Delete" asp-route-id="@item.ID">Görevleri Sil</a>
            </td>
        </tr>
}

    </tbody>

</table>
 <a class="btn btn-success" asp-action="Index" asp-controller="ToDo">Yapılmayan Görev listesine Dön</a>